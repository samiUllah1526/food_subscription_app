import Layout from "./layout";
import { TextInput, PasswordInput, Checkbox } from "./inputFields"
import Button from "./buttons";


const inputs = {
    TextInput,
    PasswordInput,
    Checkbox,

}

const buttons = {
    Button,
}

export {
    Layout,
    inputs,
    buttons,
}
